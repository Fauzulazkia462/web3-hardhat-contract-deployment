{
	"name": "web3-hardhat-contract-deployment",
	"author": "Fauzul Azkia",
	"version": "0.1.0",
	"private": true,
	"scripts": {
		"build": "npx hardhat compile",
		"deploy-local": "npx hardhat run scripts/deploy.js",
		"deploy-testnet": "npx hardhat run scripts/deploy.js --network sepolia",
		"node": "npx hardhat node"
	},
	"dependencies": {
		"dotenv": "^16.0.2"
	},
	"devDependencies": {
		"@nomicfoundation/hardhat-toolbox": "^1.0.2",
		"hardhat": "^2.10.1"
	}
}